- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install required packages
  ansible.builtin.apt:
    name:
      - curl
      - git
      - jq
      - unzip
      - ufw
      - logrotate
    state: present
    install_recommends: false
